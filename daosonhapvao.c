#include"stdio.h"

#include <math.h>

int main()
{
	int S,a,c=0,n,k;
int b;
printf("nhap 1 so:");
scanf("%d",&n);
 printf("mang dao:");
 while (n > 0)
    {
        int t = n % 10;

       printf("%d",t);

        n = n / 10;
    }

 
	}
