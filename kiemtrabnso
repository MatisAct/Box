#include"stdio.h"

#include <math.h>
int main()
{
	int a,b,c=0,n;
	printf("nhap vao 1 so bat ki:");
	scanf("%d",&n);
	for (int i=0;i<=10;i++)
	{
	b=n/pow(10,i);

	if(b!=0)
	c=c+1;
	if(b==0)
	break;
	
	}
printf("so ban vua nhap co %d chu so",c);
	}
